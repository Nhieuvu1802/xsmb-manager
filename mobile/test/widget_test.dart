import 'package:flutter_test/flutter_test.dart';
import 'package:xsmb_manager/main.dart';

void main() {
  test('constructs the application root', () {
    expect(const XsmbManagerApp(), isA<XsmbManagerApp>());
  });
}
