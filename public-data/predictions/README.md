# Predictions Data

Top4 predictions được tính real-time bởi Worker từ statistics.

Endpoint:
- `GET /v1/predictions/top4` — Top 4 tham khảo thống kê

Disclaimer: Kết quả thống kê dựa trên dữ liệu lịch sử,
không đảm bảo kết quả kỳ tiếp theo.
